# Run lint-staged to check and fix TypeScript/ESLint/Prettier issues
pnpm exec lint-staged

# Show staged files for transparency
echo ""
echo "📋 Files staged for commit:"
git diff --cached --name-only
