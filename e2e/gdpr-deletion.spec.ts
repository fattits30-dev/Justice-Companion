/**
 * Electron-specific GDPR deletion E2E tests have been retired.
 * This placeholder remains so tooling that references the spec path continues to work.
 * Re-enable the suite by restoring the previous implementation and removing this stub.
 */
export {};
