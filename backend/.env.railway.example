# Justice Companion - Railway Deployment Environment Variables
# These environment variables should be set in Railway dashboard

# ============================================================================
# REQUIRED: Set these in Railway
# ============================================================================

# Security & Encryption
ENCRYPTION_KEY_BASE64=your_encryption_key_here

# AI Configuration
AI_PROVIDER=huggingface
AI_API_KEY=your_huggingface_api_key_here
AI_MODEL=meta-llama/Meta-Llama-3.1-70B-Instruct

# CORS Configuration (set AFTER you deploy frontend)
ALLOWED_ORIGINS=https://your-pwa-domain.netlify.app

# ============================================================================
# AUTOMATICALLY PROVIDED BY RAILWAY (Do not set manually)
# ============================================================================

# Railway automatically provides:
# - DATABASE_URL (when you add PostgreSQL plugin)
# - PORT (randomly assigned)
# - RAILWAY_STATIC_URL (your deployment URL)

# ============================================================================
# OPTIONAL OVERRIDES
# ============================================================================

# Host (Railway uses 0.0.0.0 by default, but you can override)
HOST=0.0.0.0

# Reload (should be false in production)
RELOAD=false
