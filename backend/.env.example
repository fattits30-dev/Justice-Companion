# ============================================================================
# Justice Companion Backend Configuration
# ============================================================================

# Server Configuration
# ----------------------------------------------------------------------------
HOST=0.0.0.0  # Use 0.0.0.0 for cloud deployment, 127.0.0.1 for local only
PORT=8000
ALLOWED_ORIGINS=*  # Development: *, Production: https://your-domain.com

# Rate Limiting Configuration (User-Controlled Security)
# ----------------------------------------------------------------------------
# IMPORTANT: Rate limiting is DISABLED by default to allow smooth development
# and testing. Enable it only when deploying to production or shared environments.
#
# When to ENABLE rate limiting:
#   - Public-facing deployments (internet-accessible)
#   - Shared hosting environments
#   - Cloud deployments (Railway, Heroku, AWS, etc.)
#   - Multi-user production instances
#
# When to KEEP DISABLED:
#   - Local development
#   - E2E testing
#   - CI/CD pipelines
#   - Single-user desktop deployments
#   - Private network deployments
#
# See RATE_LIMITING_GUIDE.md for detailed security recommendations.
#
# To enable: Set ENABLE_RATE_LIMITING=true
ENABLE_RATE_LIMITING=false

# Rate Limit Configuration (applies only when ENABLE_RATE_LIMITING=true)
# ----------------------------------------------------------------------------
# Registration: Prevent mass account creation from single IP
RATE_LIMIT_REGISTER_MAX_REQUESTS=3
RATE_LIMIT_REGISTER_WINDOW_SECONDS=3600  # 1 hour

# Login: Prevent brute-force password attacks
RATE_LIMIT_LOGIN_MAX_REQUESTS=5
RATE_LIMIT_LOGIN_WINDOW_SECONDS=900  # 15 minutes

# Password Change: Prevent abuse of password change functionality
RATE_LIMIT_PASSWORD_MAX_REQUESTS=5
RATE_LIMIT_PASSWORD_WINDOW_SECONDS=3600  # 1 hour

# Database Configuration
# ----------------------------------------------------------------------------
# Default: SQLite (local file-based database)
# Production: PostgreSQL recommended for cloud deployments
DATABASE_URL=sqlite:///./justice_companion.db

# Encryption Configuration
# ----------------------------------------------------------------------------
# CRITICAL: Generate a unique encryption key for production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# WARNING: Changing this key will invalidate all encrypted data!
ENCRYPTION_KEY=your-secret-encryption-key-here-change-in-production

# Session Configuration
# ----------------------------------------------------------------------------
SESSION_EXPIRY_HOURS=24  # Default session expiration (24 hours)
REMEMBER_ME_DAYS=30  # "Remember me" session duration (30 days)

# Logging Configuration
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
