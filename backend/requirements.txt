# Justice Companion Python Backend Dependencies

# FastAPI framework
fastapi>=0.115.0  # Modern, fast (high-performance) web framework for building APIs - SECURITY UPDATE
uvicorn==0.31.1  # ASGI server

# Database
sqlalchemy==2.0.31  # SQL Toolkit and Object Relational Mapper
alembic==1.13.2    # Database migration tool for SQLAlchemy
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python
asyncpg==0.29.0  # Async PostgreSQL driver

# Async HTTP client
aiohttp==3.12.14  # Async HTTP client

# Security and Hashing
passlib==1.7.4  # Password hashing library
bcrypt==4.1.3  # Modern password hashing for your software and your servers
authlib>=1.3.3  # Secure JOSE/JWT toolkit replacing vulnerable python-jose/ecdsa stack - SECURITY UPDATE
cryptography==45.0.7  # Cryptographic recipes and primitives

# PDF and Document Processing
pypdf>=5.0.0  # PDF processing library - SECURITY UPDATE
pdfplumber==0.11.2  # Plumb a PDF for detailed information about each text character, rectangle, and line.
# pdfminer.six kept at version compatible with pdfplumber for now

# Environment and Configuration
python-dotenv==1.1.0  # Reads key-value pairs from a .env file and can set them as environment variables

# Multipart form parsing
python-multipart>=0.0.10  # Streaming multipart parser - SECURITY UPDATE

# AI and Machine Learning - SECURITY UPDATES
torch>=2.6.0  # CRITICAL: Fixes buffer overflow, deserialization RCE, memory corruption - UPDATED SECURITY
transformers>=4.47.0  # Fixes 8+ high severity ReDoS and deserialization vulnerabilities - UPDATED SECURITY
sentence-transformers>=3.3.1  # Available secure version
faiss-cpu==1.8.0.post1  # Library for efficient similarity search and clustering of dense vectors
# faiss-gpu==1.7.2 # Optional: for GPU-accelerated FAISS, requires CUDA
huggingface-hub==0.26.5  # HuggingFace Hub API for model/dataset access
openai==1.58.1  # OpenAI Python client library (for OpenAI-compatible APIs)

# System keyring for secure credential storage
keyring==25.2.1  # Cross-platform keyring access
pydantic-settings==2.2.1  # Pydantic settings management

# Other utilities
requests==2.32.5  # Elegant and simple HTTP library for Python
urllib3>=2.3.0  # Powerful, user-friendly HTTP client for Python - SECURITY UPDATE
httpx==0.28.1 # Async HTTP client, for compatibility with other packages
defusedxml==0.7.1  # Hardened XML parsing to prevent XXE/DoS attacks
