# Justice Companion Environment Variables
# Copy this file to .env and fill in the values
# NEVER commit the .env file to version control!

# ============================================
# ENCRYPTION KEY (CRITICAL SECURITY)
# ============================================
# 256-bit (32 byte) encryption key for AES-256-GCM
# Used to encrypt sensitive legal data, PII, and documents at rest
#
# TO GENERATE A NEW KEY:
# Run this in Node.js REPL or create a script:
#   const crypto = require('crypto');
#   console.log('ENCRYPTION_KEY_BASE64=' + crypto.randomBytes(32).toString('base64'));
#
# SECURITY WARNINGS:
# - Keep this key SECRET and SECURE
# - NEVER commit this key to git (env is gitignored)
# - NEVER log this key in application code
# - If key is lost, encrypted data CANNOT be recovered
# - If key is compromised, ALL encrypted data is compromised
# - Rotate keys periodically using EncryptionService.rotateKey()
#
ENCRYPTION_KEY_BASE64=REPLACE_WITH_GENERATED_KEY_FROM_CRYPTO_RANDOMBYTES

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Path to SQLite database file (relative to project root)
# DATABASE_PATH=./justice-companion.db

# ============================================
# AI SERVICE CONFIGURATION
# ============================================
# LM Studio API endpoint (if using local AI)
# LM_STUDIO_URL=http://localhost:1234/v1

# ============================================
# DEVELOPMENT SETTINGS
# ============================================
# Enable development mode features
# NODE_ENV=development

# MCP Dev API Server Port (for MCP tools integration)
# MCP_DEV_API_PORT=5555
